{
  "name": "kmclient",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "debug": "electron . --debug",
    "pack": "electron-packager . km --win --out ./build --arch=x64 --app-version=0.0.1 --electron-version=8.1.1 --icon=back64.ico"
  },
  "author": "xuchg1",
  "license": "ISC",
  "devDependencies": {
    "electron": "^8.1.1"
  },
  "dependencies": {
    "lowdb": "^1.0.0"
  }
}
